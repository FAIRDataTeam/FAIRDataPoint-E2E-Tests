language: node_js

node_js:
  - 8.16

# if using Ubuntu 16 need this library
# https://github.com/cypress-io/cypress-documentation/pull/1647
addons:
  apt:
    packages:
      - libgconf-2-4
  hosts:
    - fdp-client

cache:
  directories:
    - ~/.npm
    - ~/.cache

script:
  - make ci
